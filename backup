<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>{{ user.username }}'s Profile</title>
</head>
<body>
   <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #001f3d;">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">ProjectHub</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/browse">Browse Projects</a>
                </li>
                {% if session.get('role') == 'organizer' %}
                    <!-- Organizer-only option -->
                    <li class="nav-item">
                        <a class="nav-link" href="/upload">Upload Project</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="/contact">Contact</a>
                </li>
            </ul>

            <!-- Conditional rendering for user login status -->
            <div class="d-flex">
                {% if not session.get('user') %}
                    <!-- If user is not logged in -->
                    <a href="/auth/login" class="btn btn-outline-light me-2">Login</a>
                    <a href="/auth/register" class="btn btn-light">Sign Up</a>
                {% else %}
                    <!-- If user is logged in -->

                    <a href="/user/profile" class="btn btn-light ms-2">Profile</a>
                    <a href="/project/logout" class="btn btn-outline-light ms-2">Log Out</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>

    <!-- Profile Section -->
    <div class="text-center">
        <h3>{{ user.username }}'s Profile</h3>
    </div>

    <!-- Editable Details -->
    <div class="card shadow-sm mb-4">
        <!-- You can add more user information here if needed -->
    </div>

<div class="container mt-5">
    <h1 class="text-center">Browse Projects</h1>
    <div class="row mt-5">
        {% for project in projects %}
        <div class="col-md-4 mb-4">
            <div class="card shadow-sm">
                <img src="{{ url_for('static', filename='uploads/' + project.image) }}" class="card-img-top" alt="Project Image">
                <div class="card-body">
                    <h5 class="card-title">{{ project.title }}</h5>
                    <p class="card-text">{{ project.description[:100] }}...</p>

                    <!-- Display Username by looking up organizer_id -->
                    {% set organizer = users.get(project.organizer_id) %}
                    <p class="card-text text-muted" style="font-size: 0.9rem;">Posted by: {{ organizer.username }}</p> <!-- Display the username -->

                    <a href="{{ url_for('project_details', project_id=project.id) }}" class="btn btn-primary">View Details</a>

                    {% if session.get('user') %}
                        {% if session.get('role') == 'user' %}
                            <a href="{{ url_for('project_bp.add_to_my_projects', project_id=project.id) }}" class="btn btn-success mt-2">Add to My Projects</a>
                        {% endif %}
                    {% endif %}
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>




    <!-- Logout Button -->
    <div class="text-center mt-4">
        <a href="{{ url_for('auth.login') }}" class="btn btn-danger">Logout</a>
    </div>

</body>
</html>
